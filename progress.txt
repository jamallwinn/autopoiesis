AUTOPOIESIS — BUILD PROGRESS LOG
Format: [timestamp] STORY — status — note
Real, terminal-verifiable evidence for each item lives in that story's own Verification section
under docs/stories/. This file is the running index of what's been attempted and where things
stand — not a substitute for the per-story proof.

2026-09-03 21:48 — Story 0 — STARTED — beginning Task 1 (eligibility preflight)
2026-09-03 21:52 — Story 0 Task 1 — DONE — re-fetched live Devpost rules page. No confirmed
  conflicts. Two items need human self-certification: geographic residency, employment/family
  relation to organizers or judges. Deadline reconfirmed Oct 30 2026 10:00 AM PDT. Video-length
  wording <3:00 (not ≤3:00) reconfirmed correct. Full findings in
  docs/stories/epic-1-story-0.md Verification section.
2026-09-03 21:52 — Story 0 — starting Task 2 (shared contract document)
2026-09-03 21:58 — Story 0 Task 2 — DONE — wrote docs/architecture/shared-contract.md v0.1:
  ToolInvocation/ToolResult, acp.*/coding.* namespace split + dispatcher, write_file schema,
  run_command/run_tests schema, SandboxAdapter interface, VerificationResult submission gate,
  dashboard event contract (8 event types + monotonic seq), job correlation ID shape.
2026-09-03 21:58 — Story 0 — starting Task 3 (deployment target)
2026-09-03 22:05 — Story 0 Task 3 — DONE — chose GitHub Pages (gh CLI already authenticated;
  npx vercel hung on an interactive prompt and was abandoned rather than retried). git init'd
  this project, confirmed .env excluded via .gitignore, committed, created public repo
  jamallwinn/autopoiesis, pushed, enabled Pages. Built and confirmed reachable via WebFetch
  (off-device): https://jamallwinn.github.io/autopoiesis/ serving the real placeholder page.
  Ran a full-history secret scan before/after publishing — clean, no secrets found.
2026-09-03 22:05 — Story 0 Task 4 — DONE — confirmed shared-contract.md cross-referenced in
  Stories 0, 2, 3, 6, 8 via grep.
2026-09-03 22:06 — Story 0 — COMPLETE — all 4 tasks, all 4 acceptance criteria verified with
  real evidence in docs/stories/epic-1-story-0.md. Flagged forward to Story 8: GitHub Pages is
  static-only, a backend host (job API + SSE) still needs to be chosen there — considering a
  Nebius AI Cloud instance/DevPod to also reinforce the mandatory-stack story.
  Also flagged to the human: residency and judge/employment-relation eligibility items need
  self-certification (I can't verify these).
2026-09-03 22:06 — Repo: initial commit + push to https://github.com/jamallwinn/autopoiesis
  (public). Story 1 can now begin.

2026-09-04 00:00 — Story 1 — STARTED — beginning Task 1 (resolve base-URL discrepancy)
2026-09-04 00:04 — Story 1 Task 1 — DONE — both candidate URLs return HTTP 200, but they are
  NOT aliases: api.tokenfactory.nebius.com returns 25 models (superset), .us-central1. variant
  returns 22 (strict subset, 3 fewer: Kimi-K3, GLM-5.2, DeepSeek-V4-Pro — none are Nemotron).
  All 6 real Nemotron variants present on both. Declared api.tokenfactory.nebius.com canonical
  (superset + matches primary docs URL).
2026-09-04 00:06 — Story 1 Task 2 — DONE — confirmed nebius_api_key in .env is live/valid (used
  successfully in Task 1's calls). Appended NEBIUS_API_KEY as an uppercase alias to .env via a
  bash command that never printed the value to output/context. Re-confirmed .env still gitignored
  and untracked after the edit. Console API-key-generation UI location not visited (not needed —
  existing key already confirmed live).
2026-09-04 00:08 — Story 1 Task 3 — DONE — enumerated all 6 real Nemotron model IDs from the live
  /v1/models response (see story file for exact strings). Updating research/RESEARCH.md §7.1 now.
2026-09-04 00:09 — Story 1 Task 4 — ATTEMPTED, not conclusively confirmed — no documented Nebius
  billing/usage API endpoint exists; a best-effort guess at one (v1/dashboard/billing/usage)
  returned real HTTP 404. Since the story's own risk note says not to block on this, and the
  live API calls in Task 1/5 already succeeded (proving the account is functional), this is left
  as a manual follow-up for the human via the Nebius console rather than blocking the story.
2026-09-04 00:10 — Story 1 Task 5 — DONE — real chat-completion smoke test against
  nvidia/nemotron-3-super-120b-a12b succeeded, HTTP 200, real model response returned.
2026-09-04 00:11 — Story 1 — COMPLETE — all 5 acceptance criteria verified with real evidence.
  Task 4 (billing confirmation) flagged as a non-blocking manual follow-up for the human.
2026-09-04 00:11 — Committed + pushed Story 1 (a050097).

2026-09-04 00:15 — Story 2 — STARTED — beginning Task 1 (scaffold Node/TypeScript project)
2026-09-04 00:16 — Story 2 Task 1 — package.json created (app/), npm install of
  openai/typescript/tsx/dotenv running much longer than expected (first attempt timed out at 2min,
  retried and moved to background). Waiting on it before continuing — not proceeding on an
  unconfirmed dependency state.
2026-09-04 00:25 — Story 2 Task 1 — DONE — npm install completed in background (openai 7.10.0,
  typescript 7.0.2, tsx, dotenv). tsconfig.json added. Confirmed .env loading picks up
  NEBIUS_API_KEY via app/scripts/check-env.ts.
2026-09-04 00:30 — Story 2 Task 3 (tool-calling mode) — DONE — real request with coding-tool
  schema returned real OpenAI-shaped tool_calls array, finish_reason: tool_calls. NATIVE mode
  confirmed, no fallback JSON mode needed.
2026-09-04 00:33 — Story 2 Task 2 (normalizer) — DONE — wrote app/src/normalizer.ts, unit-tested
  against the real captured response saved to disk from the Task 3 run (not a hand fixture) —
  passed.
2026-09-04 00:40 — Story 2 Task 3 (runAgentTask) — DONE — implemented app/src/agentLoop.ts with
  iteration cap, wall-clock cap, cancellation, defined terminal states. Ran 3 distinct prompts:
  3/3 completed via real multi-turn write->test->summarize cycles. Deliberate iteration-cap test
  (maxIterations=1) correctly stopped a real run rather than hanging.
2026-09-04 00:45 — Story 2 Task 4 (failure-mode matrix) — first run 5/6, REAL BUG FOUND: timeout
  classification checked err.name but the openai SDK sets err.constructor.name instead — every
  real timeout was silently misclassified as non-retryable. Root-caused with a one-off debug
  script (removed after use), fixed in app/src/retry.ts, re-ran: 6/6 passed. Re-ran
  test-agent-loop.ts afterward as a regression check — still 3/3, no regression from the fix.
2026-09-04 00:50 — Story 2 — COMPLETE — all 6 acceptance criteria verified with real evidence,
  including the timeout bug found+fixed via the story's own testing (not glossed over). Scope
  discipline confirmed: no Contree/Virtuals/chain references anywhere in app/.

2026-09-04 01:10 — Sandbox readiness check (pre-Story-3) — installed contree-mcp via uv; hit a
  real packaging bug (contree-mcp 0.4.0 declares open-ended `mcp>=1.0.0`, resolved to mcp==2.1.1
  which removed the `FastMCP` re-export contree-mcp's code imports from `mcp.server` — crashed on
  `--help`). Fixed by pinning `mcp<2.0.0` via `uv tool install contree-mcp --with "mcp<2.0.0"
  --force` (resolved to mcp==1.29.1); `contree-mcp --help`/`--version` then worked.
  Registered as an MCP server via `claude mcp add` (takes effect next session, not this one).
  Direct REST probe against https://api.tokenfactory.nebius.com/sandboxes/v1/operations:
  no Project header -> 400; with NEBIUS_AI_PROJECT header (project ID provided by the human,
  aiproject-e00cnhtcrxav2x0x2x) -> 403 "Insufficient permissions: list". Confirmed via a second
  probe this isn't specific to the list action (POST -> 405 Method Not Allowed on that path,
  a different resource path -> 404) — it's an account-level gate.
  **Human found the actual cause**: Token Factory Sandboxes Beta requires manual enrollment via a
  Microsoft Forms request (project ID + email + use-case description), reviewed by Nebius before
  access is granted. This is NOT a bug on our side and NOT fixable via API/CLI — it's a real,
  external approval step. Sandbox is confirmed NOT YET READY; Story 3 is blocked until enrollment
  is approved. Added NEBIUS_AI_PROJECT to .env.
2026-09-04 01:20 — Human submitted the Beta enrollment form (project aiproject-e00cnhtcrxav2x0x2x,
  jamallwinn@gmail.com, use-case description re: coding agent + sandboxed test-verified job
  submission for the hackathon).
2026-09-04 01:22 — Re-checked sandbox access post-submission: still HTTP 403 "Insufficient
  permissions: list" — identical to pre-submission. Expected, not a failure: the form states
  Nebius will notify by email once approved, implying manual review turnaround rather than instant
  activation. Story 3 remains blocked pending that approval; nothing further to do here until
  access is granted. Track B (Stories 4/5, Virtuals identity/token) has no dependency on this and
  can proceed in parallel per the epic's build-order.
